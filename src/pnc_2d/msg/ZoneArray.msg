# 一组地图区域（禁行区 + 限速区），latched 发布（"地图的语义层"）。
#
# ★ `inflate` = **这组几何该按多大膨胀量使用** [m]，与 map_server 烧全局图用的
#   是同一个值；消费者必须照用，否则两边对"能不能过"的判定会不一致。
#   （踩过：全局按车体外接圆 0.472 m 烧进图，局部又算一遍车体轮廓 ⇒ 实际禁入带
#   0.7~1.0 m，车停在合法位置却怎么都规划不出来。）
#   现在统一用一个参数：`map_server` 的 `zones.inflate`（0 = 只靠车体轮廓精确判定）。
std_msgs/Header header
float64 inflate
Zone[] zones
